# Frozen operational dependency for the post-holdout state-signal ablation.
# Gymnasium is used only for Env/Box interfaces; no packaged environment,
# wrapper, sampler, or reward implementation enters the portfolio algorithm.
gymnasium==1.3.0
# Required only by the no-holdout diagnostic exporter. It is deliberately
# installed in the same pinned GPU Python selected by RETICULATE_PYTHON.
pandas==3.0.5
